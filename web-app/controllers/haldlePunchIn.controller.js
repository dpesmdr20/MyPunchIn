/**
 * Created by dimanandhar on 8/9/17.
 */
angular.module('punchInController',[])
.controller('PunchInController',['$scope','$http',function($scope,$http) {
    /*$http.post('/punchIn', data, config)
        .success(function (data, status, headers, config) {
            $scope.PostDataResponse = data;
        })
        .error(function (data, status, header, config) {
            $scope.ResponseDetails = "Data: " + data +
                "<hr />status: " + status +
                "<hr />headers: " + header +
                "<hr />config: " + config;
        });*/
}])
